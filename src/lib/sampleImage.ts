/**
 * The bundled demo photo, served from public/. One home for the path: the
 * editor's "Try a sample" and the Effect Creator's test image both load it
 * (via urlToFile, which keeps the canvas untainted and exportable).
 */
export const SAMPLE_IMAGE_URL = '/pink-car-space-needle4.jpg'
